local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ContentProvider = game:GetService("ContentProvider")
local StarterGui = game:GetService("StarterGui")
local TeleportService = game:GetService("TeleportService")
local RunService = game:GetService("RunService")
local PhysicsService = game:GetService("PhysicsService")
local SoundService = game:GetService("SoundService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer.PlayerGui
local gameId = game.PlaceId

-- Kiểm tra game
local supportedGames = {
    [2753915549] = "Blox Fruits",
    [1234567890] = "99 Đêm" -- Thay bằng PlaceId thực tế của 99 Đêm
}
if not supportedGames[gameId] then
    StarterGui:SetCore("SendNotification", {
        Title = "Thông Báo",
        Text = "Cảm ơn bạn đã sử dụng script của mình nhé 😘",
        Duration = 10
    })
end

-- Phát hiện admin và hop server
local function checkAdmin()
    local adminIds = {[912348] = true, [120173604] = true}
    Players.PlayerAdded:Connect(function(player)
        if adminIds[player.UserId] then
            if player.UserId == 120173604 then
                pcall(function()
                    TeleportService:TeleportToPlaceInstance(gameId, game.JobId, LocalPlayer)
                end)
            else
                StarterGui:SetCore("SendNotification", {
                    Title = "Cảnh Báo",
                    Text = "Admin vô server sẽ đổi server trong 5s nữa",
                    Duration = 5
                })
                wait(5)
                pcall(function()
                    TeleportService:TeleportToPlaceInstance(gameId, game.JobId, LocalPlayer)
                end)
            end
        elseif player.Name:lower():find("admin") or player:GetRoleInGroup(game.CreatorId) ~= "Guest" then
            StarterGui:SetCore("SendNotification", {
                Title = "Cảnh Báo",
                Text = "Admin vô server sẽ đổi server trong 5s nữa",
                Duration = 5
            })
            wait(5)
            pcall(function()
                TeleportService:TeleportToPlaceInstance(gameId, game.JobId, LocalPlayer)
            end)
        end
    end)
end
pcall(checkAdmin)

-- Tải UI Redz V2
pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/daucobonhi/Ui-Redz-V2/refs/heads/main/UiREDzV2.lua"))()
end)

-- Preload ảnh
pcall(function()
    ContentProvider:PreloadAsync({
        "rbxassetid://756765780901",
        "rbxassetid://75676578090181",
        "rbxassetid://89326205091486"
    })
end)

-- Intro animation
local function introAnimation()
    local screenGui = Instance.new("ScreenGui", PlayerGui)
    screenGui.Name = "IntroGui"
    screenGui.IgnoreGuiInset = true
    local frame = Instance.new("Frame", screenGui)
    frame.Size = UDim2.new(0, 300, 0, 150)
    frame.Position = UDim2.new(0.5, -150, 0.5, -75)
    frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    frame.BackgroundTransparency = 0.5
    local textLabel = Instance.new("TextLabel", frame)
    textLabel.Size = UDim2.new(1, 0, 0.6, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = "Kirada VN Hack\nTác giả: Kirada & Habato\nNgười làm & viết: Kirada & Hiếu TV"
    textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    textLabel.TextScaled = true
    local imageLabel = Instance.new("ImageLabel", frame)
    imageLabel.Size = UDim2.new(0, 100, 0, 100)
    imageLabel.Position = UDim2.new(0.5, -50, 0.6, 0)
    imageLabel.BackgroundTransparency = 1
    imageLabel.Image = "rbxassetid://75676578090181"
    imageLabel.ImageTransparency = 1
    local tweenInfo = TweenInfo.new(1, Enum.EasingStyle.Sine)
    local fadeInText = TweenService:Create(textLabel, tweenInfo, {TextTransparency = 0})
    local fadeInFrame = TweenService:Create(frame, tweenInfo, {BackgroundTransparency = 0.2})
    local fadeInImage = TweenService:Create(imageLabel, tweenInfo, {ImageTransparency = 0})
    local fadeOutText = TweenService:Create(textLabel, tweenInfo, {TextTransparency = 1})
    local fadeOutFrame = TweenService:Create(frame, tweenInfo, {BackgroundTransparency = 1})
    local fadeOutImage = TweenService:Create(imageLabel, tweenInfo, {ImageTransparency = 1})
    fadeInText:Play()
    fadeInFrame:Play()
    fadeInImage:Play()
    fadeInText.Completed:Wait()
    wait(5)
    fadeOutText:Play()
    fadeOutFrame:Play()
    fadeOutImage:Play()
    fadeOutText.Completed:Wait()
    screenGui:Destroy()
end
pcall(introAnimation)

-- Tạo menu
local window = MakeWindow({
    Hub = {Title = "Kirada VN", Animation = "YouTube: Kirada VN"},
    Key = {KeySystem = false, Title = "Key System", Keys = {"1234"}, Notifi = {Notifications = true, CorrectKey = "Running the Script...", Incorrectkey = "The key is incorrect", CopyKeyLink = "Copied to Clipboard"}}
})
MinimizeButton({
    Image = "rbxassetid://89326205091486",
    Size = {60, 60},
    Color = Color3.fromRGB(10, 10, 10),
    Corner = true,
    Stroke = false,
    StrokeColor = Color3.fromRGB(255, 0, 0)
})

-- Danh sách script đã chạy
local executedScripts = {}

-- Hàm thêm nút
local function addButton(tab, name, url, source)
    AddButton(tab, {
        Name = name,
        Callback = function()
            local success, err = pcall(function()
                if url:find("http") then
                    loadstring(game:HttpGet(url))()
                    table.insert(executedScripts, name .. (source and " (" .. source .. ")" or ""))
                else
                    setclipboard(url)
                end
            end)
            StarterGui:SetCore("SendNotification", success and {
                Title = source or name,
                Text = "Đã " .. (url:find("http") and "chạy script " or "sao chép link ") .. (source or name) .. " vào clipboard!" .. (source and " Nguồn: " .. source or ""),
                Duration = url:find("http") and 5 or 10
            } or {
                Title = "Lỗi",
                Text = "Không thể " .. (url:find("http") and "chạy script " or "sao chép link ") .. name .. ": " .. tostring(err),
                Duration = 5
            })
        end
    })
end

-- Tab: Blox Fruit
local tab1 = MakeTab({Name = "Blox Fruit"})
addButton(tab1, "W-AZURE", "https://api.luarmor.net/files/v3/loaders/85e904ae1ff30824c1aa007fc7324f8f.lua")
addButton(tab1, "H4X Script", "https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua")
addButton(tab1, "Nat Hub", "https://get.nathub.xyz/loader")
addButton(tab1, "Quantum Hub", "https://raw.githubusercontent.com/flazhy/QuantumOnyx/refs/heads/main/QuantumOnyx.lua")
addButton(tab1, "Speed Hub", "https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua")
addButton(tab1, "OMG HUB sever vip free (getkey)", "https://pastefy.app/US0MtPIY/raw")
addButton(tab1, "Giảm Lag", "https://raw.githubusercontent.com/TurboLite/Script/main/FixLag.lua")

-- Tab: Hop Server
local tab2 = MakeTab({Name = "Hop Server"})
addButton(tab2, "Teddy Hub (getkey)", "https://raw.githubusercontent.com/Teddyseetink/Haidepzai/refs/heads/main/TEDDYHUB-FREEMIUM")

-- Tab: 99 Đêm
local tab3 = MakeTab({Name = "99 Đêm"})
addButton(tab3, "NATHUB (getkey)", "https://get.nathub.xyz/loader")
addButton(tab3, "H4X (getkey)", "https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua")
addButton(tab3, "Speed Hub (getkey)", "https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua")
addButton(tab3, "Hack Farm Kim Cương", "https://raw.githubusercontent.com/sleepyvill/script/refs/heads/main/99nights.lua")
addButton(tab3, "Skibidi", "https://raw.githubusercontent.com/caomod2077/Script/refs/heads/main/FoxnameHub.lua")

-- Tab: Key System
local tab5 = MakeTab({Name = "Key System"})
addButton(tab5, "Copy Speed Hub Key", "KfHLmNFnuaRmvbkQRwZGXDROXkxhdYAE", "Key System")

-- Tab: Idol YTB Hack
local tab6 = MakeTab({Name = "Idol YTB Hack"})
addButton(tab6, "Copy Vịt Lỏd Link", "https://www.youtube.com/channel/UCQinN9_tN8ln_Mk3hqEfLLw", "Vịt Lỏd")
addButton(tab6, "Copy EZ AK Gaming Link", "https://www.youtube.com/@akgamingytb999", "EZ AK Gaming")
addButton(tab6, "Copy TBoy Link", "https://www.youtube.com/channel/UCTwTw3BeiQm2dNtsVeCxlYw", "TBoy")

-- Tab: Cài Đặt (mới)
local tab7 = MakeTab({Name = "Cài Đặt"})
local isMuted = false
addButton(tab7, "Tự Chạy Tất Cả Script", "", nil, function()
    local scripts = {
        {"W-AZURE", "https://api.luarmor.net/files/v3/loaders/85e904ae1ff30824c1aa007fc7324f8f.lua"},
        {"H4X Script", "https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua"},
        {"Nat Hub", "https://get.nathub.xyz/loader"},
        {"Quantum Hub", "https://raw.githubusercontent.com/flazhy/QuantumOnyx/refs/heads/main/QuantumOnyx.lua"},
        {"Speed Hub", "https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua"},
        {"OMG HUB sever vip free (getkey)", "https://pastefy.app/US0MtPIY/raw"},
        {"Giảm Lag", "https://raw.githubusercontent.com/TurboLite/Script/main/FixLag.lua"},
        {"Teddy Hub (getkey)", "https://raw.githubusercontent.com/Teddyseetink/Haidepzai/refs/heads/main/TEDDYHUB-FREEMIUM"},
        {"NATHUB (getkey)", "https://get.nathub.xyz/loader"},
        {"H4X (getkey)", "https://raw.githubusercontent.com/H4xScripts/Loader/refs/heads/main/loader.lua"},
        {"Speed Hub (getkey)", "https://raw.githubusercontent.com/AhmadV99/Speed-Hub-X/main/Speed%20Hub%20X.lua"},
        {"Hack Farm Kim Cương", "https://raw.githubusercontent.com/sleepyvill/script/refs/heads/main/99nights.lua"},
        {"Skibidi", "https://raw.githubusercontent.com/caomod2077/Script/refs/heads/main/FoxnameHub.lua"}
    }
    for _, script in ipairs(scripts) do
        local success, err = pcall(function()
            loadstring(game:HttpGet(script[2]))()
            table.insert(executedScripts, script[1])
        end)
        StarterGui:SetCore("SendNotification", success and {
            Title = "Tự Chạy Script",
            Text = "Đã chạy script " .. script[1] .. " thành công!",
            Duration = 5
        } or {
            Title = "Lỗi",
            Text = "Không thể chạy script " .. script[1] .. ": " .. tostring(err),
            Duration = 5
        })
        wait(1) -- Đợi để tránh quá tải
    end
end)
addButton(tab7, "Xem Script Đã Chạy", "", nil, function()
    local scriptList = #executedScripts > 0 and table.concat(executedScripts, "\n") or "Chưa chạy script nào!"
    StarterGui:SetCore("SendNotification", {
        Title = "Script Đã Chạy",
        Text = scriptList,
        Duration = 10
    })
end)
addButton(tab7, "Tắt/Bật Âm Thanh", "", nil, function()
    isMuted = not isMuted
    SoundService.Volume = isMuted and 0 or 0.5
    StarterGui:SetCore("SendNotification", {
        Title = "Âm Thanh",
        Text = isMuted and "Đã tắt âm thanh game!" or "Đã bật âm thanh game!",
        Duration = 5
    })
end)

-- Tab: Mạng Xã Hội
local tab4 = MakeTab({Name = "Mạng Xã Hội"})
addButton(tab4, "Copy Discord Link", "https://discord.gg/kJ9ydA2PP4", "Discord")
addButton(tab4, "Copy YouTube Link", "https://www.youtube.com/@kiradavn", "YouTube")
addButton(tab4, "Copy TikTok Link", "https://www.tiktok.com/@offbyebyesad", "TikTok")

-- Chống anti-cheat
local function antiCheat()
    local mt = getrawmetatable(game)
    local namecall = mt.__namecall
    local index = mt.__index
    setreadonly(mt, false)
    mt.__namecall = function(self, ...)
        local method = getnamecallmethod()
        if (method == "FireServer" or method == "InvokeServer") and (tostring(self) == "AntiCheat" or tostring(self) == "AC" or tostring(self) == "FlyDetection" or tostring(self) == "HeightCheck") then
            return
        end
        return namecall(self, ...)
    end
    mt.__index = function(self, key)
        if self:IsA("Humanoid") and (key == "WalkSpeed" or key == "JumpPower" or key == "HipHeight") then
            return math.random(15, 17) + math.random()
        end
        if self:IsA("Character") and key == "Position" then
            local pos = index(self, key)
            return Vector3.new(pos.X + math.random(-0.1, 0.1), pos.Y + math.random(-0.05, 0.05), pos.Z + math.random(-0.1, 0.1))
        end
        return index(self, key)
    end
    setreadonly(mt, true)
    local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    local humanoid = character:WaitForChild("Humanoid")
    local rootPart = character:WaitForChild("HumanoidRootPart")
    PhysicsService:CreateCollisionGroup("NoClip")
    PhysicsService:CollisionGroupSetCollidable("NoClip", "Default", false)
    for _, part in pairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            PhysicsService:SetPartCollisionGroup(part, "NoClip")
        end
    end
    RunService.Heartbeat:Connect(function()
        if humanoid:GetState() == Enum.HumanoidStateType.Freefall or humanoid:GetState() == Enum.HumanoidStateType.Flying then
            rootPart.Velocity = rootPart.Velocity + Vector3.new(math.random(-1, 1)/10, math.random(-1, 1)/10, math.random(-1, 1)/10)
        end
    end)
end
pcall(antiCheat)

-- Đợi game tải
repeat task.wait() until game:IsLoaded() and PlayerGui
